/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath : '/Human-Resource-Management',
    assetPrefix : '/Human-Resource-Management/',
    trailingSlash: true,
    output : 'export',
};

export default nextConfig;
